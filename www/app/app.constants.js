/**
 * @ngdoc constants
 * @name angularApp.constant:CONSTANTS
 * @description
 * # CONSTANTS
 * CONSTANTS used for util App
 */

(function() {
    'use strict';

    angular
        .module('angularApp')
        .constant('CONSTANTS', {
            TEST_VAR: 'test const'
        });
})();
